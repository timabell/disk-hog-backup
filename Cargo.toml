[package]
name = "disk-hog-backup"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4.39"
clap = { version = "4.5.30", features = ["derive"] }
rand = "0.9.0"

[dev-dependencies]
assert_cmd = "2.0.12"
predicates = "3.0.4"
